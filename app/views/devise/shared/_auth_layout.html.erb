<div class="min-h-screen bg-gradient-to-br from-blue-50 to-gray-100 flex flex-col justify-center py-12 sm:px-6 lg:px-8">
  <div class="sm:mx-auto sm:w-full sm:max-w-md">
    <%= link_to root_path, class: "flex justify-center" do %>
      <h1 class="text-3xl font-bold text-blue-600">Map Pin App</h1>
    <% end %>
    <h2 class="mt-6 text-center text-2xl font-extrabold text-gray-900">
      <%= yield :auth_title %>
    </h2>
  </div>

  <div class="mt-8 sm:mx-auto sm:w-full sm:max-w-md">
    <div class="bg-white py-8 px-6 shadow-lg rounded-lg sm:px-10">
      <%= yield %>
    </div>
  </div>
</div>